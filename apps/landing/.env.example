# Landing-page build env (Vite). Copy to .env and fill in.

# Google Analytics 4 Measurement ID (looks like "G-XXXXXXXXXX").
# Leave empty / unset to keep analytics disabled (gtag.js is never loaded).
# Alternatively, paste the id directly into GA_MEASUREMENT_ID in src/lib/analytics.ts.
VITE_GA_ID=
